import { useRouter } from "next/navigation";
import React from "react";
import toast from "react-hot-toast";

const useToast = () => {
  const router = useRouter();

    const 

  return toastWithRedirect;
};

export default useToast;
